import { IconProps } from "@phosphor-icons/react";
import { Briefcase, Globe, GitPullRequest } from "@phosphor-icons/react";

const iconMapping: { [key: string]: React.FC<IconProps> } = {
    Briefcase,
    Globe,
    GitPullRequest,
};

export default iconMapping;
