import { IconProps, Circle, Briefcase, Globe, GitPullRequest } from "@phosphor-icons/react";

const iconMapping: { [key: string]: React.FC<IconProps> } = {
    Briefcase,
    Globe,
    GitPullRequest,
    Circle,
};

export default iconMapping;
